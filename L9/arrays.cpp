#include <bits/stdc++.h>
using namespace std;

int main()
{

    int arr[5] = {1, 2, 3, 4, 5};

    cout << sizeof(arr) / sizeof(int) << endl; // to remove size of arr we use sizeof(arr) / sizeof(int)

    return 0;
}